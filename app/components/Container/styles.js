import React from 'react';
import {
	StyleSheet
} from 'react-native'

export default StyleSheet.create({
	container: {
		flex: 1,
		backgroundColor: '#3f3834',
		alignItems: 'center',
		justifyContent: 'center',
	},
});
